import { EmptyAuthProvider } from './empty-auth-provider';

describe('EmptyAuthProvider', () => {
  it('should create an instance', () => {
    //expect(new EmptyAuthProvider()).toBeTruthy();
  });
});

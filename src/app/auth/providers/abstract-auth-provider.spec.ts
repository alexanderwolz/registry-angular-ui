import { AbstractAuthProvider } from './abstract-auth-provider';

describe('AuthProvider', () => {
  it('should create an instance', () => {
    //expect(new AbstractAuthProvider()).toBeTruthy();
  });
});

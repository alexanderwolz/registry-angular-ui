import { BasicAuthProvider } from './basic-auth-provider';

describe('BasicAuthProvider', () => {
  it('should create an instance', () => {
    //expect(new BasicAuthProvider("server",'Basic realm="myRealm"')).toBeTruthy();
  });
});
